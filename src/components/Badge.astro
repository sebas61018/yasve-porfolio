<span
  class="typewriter bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded transition-all duration-300 hover:bg-blue-800 hover:text-white hover:scale-105">
  Disponible Para tus proyectos 👈
</span>

<style>
/* Efecto de máquina de escribir */
@keyframes typing {
  from {
    width: 0;
  }
  to {
    width: 100%;
  }
}

@keyframes blink {
  50% {
    border-color: transparent;
  }
}

.typewriter {
  display: inline-block;
  overflow: hidden;
  white-space: nowrap;
  border-right: 2px solid #1e40af; /* Color azul para el cursor */
  animation: typing 3s steps(30, end), blink 0.8s step-end infinite;
}
</style>
